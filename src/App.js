import './App.css';
import Main from './components/main'; // Importing the Main component to the main page

function App() {
  return (
    <div className="App">
    <Main /> {/* Adding the Main component into the App */}
    </div>
  );
}

export default App; // I don't know where this goes tbh, think you just need to know up to here
